fastapi==0.115.*
uvicorn[standard]==0.32.*
# Pinned exactly: app/config.py and app/main.py rely on laya internals
# (Router.preload(names=...), the result's "usage" key) that can change
# between releases. Bump deliberately and re-test.
laya==0.3.5
pydantic==2.*

# Plain version pin on purpose. In Docker, the Dockerfile installs torch
# first, picking the CPU or CUDA build via the TORCH_VARIANT build arg
# (e.g. 2.14.0+cpu), and that local-version build already satisfies this
# line. Outside Docker, pip takes it from PyPI (CPU/MPS on macOS). Keep in
# sync with TORCH_VERSION in the Dockerfile.
torch==2.14.0
